#include <bits/stdc++.h>
using namespace std;

int main(){
    str ip = "1321";
    char = '1';

    for(int i = 0; i < ip.size();i++){
        if(ip[i] == d){
            str temp = ip.substr(0,i) + ip((i+1),ip.size()-1);
            vector <int>  
        }
    }
}